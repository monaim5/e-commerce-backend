from pathlib import Path

root = Path(__file__).resolve().parent.parent
app_path = root / 'src/main/java/com/app/ecommerce'
dtos_test_path = app_path / 'dto_test'
dtos_path = app_path / 'dto'
models_path = app_path / 'models'
